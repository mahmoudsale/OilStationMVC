@*@model IEnumerable<OilStationMVC.ViewModels.FinancialViewModel>*@

@using OilStationMVC.Views.Shared.Resources

@{
    int SubledgerType = OilStationMVC.Helper.ObjectConverter.CInt(ViewBag.SubledgerTypeId);
    string JournalId = OilStationMVC.Helper.ObjectConverter.Cstr(ViewBag.JournalType);
    if (JournalId == "1")
    {
        ViewData["Title"] = SiteResource.Voucher_List;
    }
    if (JournalId == "2")
    {
        ViewData["Title"] = SiteResource.Cashing_List;
    }
    if (JournalId == "6")
    {
        ViewData["Title"] = SiteResource.Car_Custody_List;
    }
    if (JournalId == "7")
    {
        ViewData["Title"] = SiteResource.Station_Deposit_List;
    }
    if (JournalId == "10")
    {
        ViewData["Title"] = SiteResource.Expenses_List;
    }
    if (JournalId == "13")
    {
        ViewData["Title"] = SiteResource.Deposit_In_Box_List;
    }
    if (JournalId == "8")
    {
        ViewData["Title"] = SiteResource.Cash_From_Box_List;
    }
    if (JournalId == "11")
    {
        ViewData["Title"] = SiteResource.Mazot_Bank_Deposit_List;
    }

}
<div class="card">
    <div class="card-header">
        @Html.ActionLink(SiteResource.Create_New, "Create", "Financial", new { JournalType = @JournalId, SubledgerTypeId = @SubledgerType }, new { @class = "btn btn-primary" })
    </div>
    <div class="card-body">
        <table id="example2" class="table table-bordered table-hover">
            <thead>
                <tr>
                    <td>@SiteResource.Code</td>
                    <td>@SiteResource.Date</td>
                    <td>@SiteResource.Journal_Type</td>
                    <td>@SiteResource.Cost_Center</td>
                    <td>@SiteResource.Amount</td>
                    <td>@SiteResource.Deposit_No</td>
                    <td>@SiteResource.Description</td>
                    <th>@SiteResource.Edit</th>
                    <th>@SiteResource.Details</th>
                    <th>@SiteResource.Delete</th>

                </tr>
            </thead>
            @*<tbody>
                    @if (Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Id</td>
                                <td>@item.dDate.ToString("yyyy-MM-dd")</td>
                                <td>@item.JournalType</td>
                                <td>@item.Subledger.Name</td>
                                <td>@item.Amount</td>
                                <td>@item.DepositNo</td>
                                <td>@item.Desc</td>
                                <td>
                                    @Html.ActionLink(SiteResource.Edit, "Edit", new { Id = item.Id }, htmlAttributes: new { @class = "btn btn-primary" })
                                </td>
                                <td>
                                    @Html.ActionLink(SiteResource.Details, "Details", new { Id = item.Id }, htmlAttributes: new { @class = "btn btn-info" })
                                </td>
                                <td>
                                    @Html.ActionLink(SiteResource.Delete, "Delete", new { Id = item.Id }, htmlAttributes: new { @class = "btn btn-danger" })
                                </td>
                            </tr>
                        }
                    }
                    else

                    {
                        <tr>
                            <td> No Car Listed</td>
                        </tr>
                    }

                </tbody>*@
            @*<tfoot>
                    <tr>
                        <td>@SiteResource.Code</td>
                        <td>@SiteResource.Date</td>
                        <td>@SiteResource.Journal_Type</td>
                        <td>@SiteResource.Cost_Center</td>
                        <td>@SiteResource.Amount</td>
                        <td>@SiteResource.Deposit_No</td>
                        <td>@SiteResource.Description</td>
                        <th>@SiteResource.Edit</th>
                        <th>@SiteResource.Details</th>
                        <th>@SiteResource.Delete</th>
                    </tr>
                </tfoot>*@
        </table>
    </div>
</div>
@section Scripts {
    @*<script src="~/assets/src/EnableDataTable.js"></script>*@
    <script>
         var JourlType =@JournalId;
         var SbldgrTypeId =@SubledgerType;
         var parms = {
            JournalType: JourlType,
            SubledgerTypeId: SbldgrTypeId,
    }


    </script>
    <script src="~/assets/src/FinancialList.js"></script>
    @*<script src="~/assets/src/CustomJavaScript.js"></script>*@
}